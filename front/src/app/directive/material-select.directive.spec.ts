import { MaterialSelectDirective } from './material-select.directive';

describe('MaterialSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new MaterialSelectDirective();
    expect(directive).toBeTruthy();
  });
});
