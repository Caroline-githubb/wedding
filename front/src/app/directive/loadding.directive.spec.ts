import { LoaddingDirective } from './loadding.directive';

describe('LoaddingDirective', () => {
  it('should create an instance', () => {
    const directive = new LoaddingDirective();
    expect(directive).toBeTruthy();
  });
});
